/** Shapes shared by the end-to-end specs. */

/** What the passes page's static shell must show, and the streamed price it must not yet. */
export type InstantExpectationType = { name: string; price: string };
